import React from 'react';

export const ElectionSummary = (election) => {
    //
    
    // return (
    //     election.questions.map(e => {
    //     <div>{e.question} | {e.yesCount}</div>
    //     })
    // );
};